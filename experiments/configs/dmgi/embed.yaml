ACM:
  alpha: 0.1
  check_val_every_n_epoch: 50
  emb_dim: 64
  lr: 0.001
  max_epochs: 2000
  model_cls: mssl.models.DMGI
  weight_decay: 0.0005
Amazon:
  alpha: 0.01
  check_val_every_n_epoch: 50
  emb_dim: 64
  lr: 0.001
  max_epochs: 2000
  model_cls: mssl.models.DMGI
  weight_decay: 0.0005
CiteSeer:
  alpha: 0.01
  check_val_every_n_epoch: 50
  emb_dim: 64
  lr: 0.001
  max_epochs: 2000
  model_cls: mssl.models.DMGI
  weight_decay: 0.0005
Cora:
  alpha: 0.001
  check_val_every_n_epoch: 50
  emb_dim: 64
  lr: 0.001
  max_epochs: 2000
  model_cls: mssl.models.DMGI
  weight_decay: 0.0005
Freebase:
  alpha: 0.0001
  check_val_every_n_epoch: 50
  emb_dim: 64
  lr: 0.001
  max_epochs: 2000
  model_cls: mssl.models.DMGI
  weight_decay: 0.0005
IMDB:
  alpha: 0.1
  check_val_every_n_epoch: 50
  emb_dim: 64
  lr: 0.001
  max_epochs: 2000
  model_cls: mssl.models.DMGI
  weight_decay: 0.0005
